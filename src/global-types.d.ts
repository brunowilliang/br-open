// Ambient declarations for globals injected by the React Native runtime.
// `__DEV__` is defined by Metro/Hermes at bundle time and is stripped from
// production bundles, but TypeScript/Biome don't know about it without this.

declare const __DEV__: boolean;
